# Copy to .env.local and fill in. Never commit real keys — .env.local is gitignored.

# OpenRouteService API key (free tier, ~500 isochrone requests/day, no card).
# Sign up: https://openrouteservice.org/dev/#/signup
# Used server-side only by the /api routes; never exposed to the browser.
ORS_API_KEY=
